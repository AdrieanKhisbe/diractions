# zsh shpec build draft
language: zsh
env:
  - SHELL=zsh
install:
  # install antigen
  - curl -L https://raw.githubusercontent.com/zsh-users/antigen/master/antigen.zsh > ~/antigen.zsh
  - echo "source ~/antigen.zsh\n antigen bundle rylnd/shpec" > ~/.zshrc
script:
  - zsh --version
    # Check if shall not load diraction by hand
  - shpec --version
  - shpec

before_install:
  - sudo apt-get install zsh
  - export SHELL=zsh

notifications:
  email: false
